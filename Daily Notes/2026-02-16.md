- C++ based on C , blocks ,statements, the preprocessor, build-in data type, arrays, pointer, etc.

- C++ using objects to manage resources
- C++ is C with classes (destructor, constructor): Abstraction(virtual function), Polymorphism, Encapsulation, Inheritance.
- Template C++ this is generic programming part of C++.
- The STL is template library, of course but it's a very special template library.